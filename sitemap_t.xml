<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type='text/xsl' href='sitemap.xsl'?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% for post in posts %}
  <url>
    <loc>{{ post.permalink | uriencode }}</loc>
    {% if post.updated %}
    <lastmod>{{ post.updated.toISOString() }}</lastmod>
    {% elif post.date %}
    <lastmod>{{ post.date.toISOString() }}</lastmod>
    {% endif %}
  </url>
  {% endfor %}
  {% for post in tags %}
  <url>
    <loc>{{ tag.permalink | uriencode }}</loc>
    {% if tag.updated %}
    <lastmod>{{ tag.updated.toISOString() }}</lastmod>
    {% elif tag.date %}
    <lastmod>{{ tag.date.toISOString() }}</lastmod>
    {% endif %}
  </url>
  {% endfor %}
  {% for post in posts %}
  <url>
    <loc>{{ post.permalink | uriencode }}</loc>
    {% if post.updated %}
    <lastmod>{{ post.updated.toISOString() }}</lastmod>
    {% elif post.date %}
    <lastmod>{{ post.date.toISOString() }}</lastmod>
    {% endif %}
  </url>
  {% endfor %}
</urlset>
